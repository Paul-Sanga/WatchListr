export interface ShowModel {
    id: number;
    name: string;
    description: string;
    poster_url: string;
}

export interface AddShowModel {
    name: string;
    description: string;
    poster_url: string;
}

